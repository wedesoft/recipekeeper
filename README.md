# Recipe Keeper conversion to Mealmaster

Converts `recipe.html` to Mealmaster recipes and prints them to standard output.

```Shell
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python main.py > output.mmf
```
